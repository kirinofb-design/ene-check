/**
 * レポート画面の「データ取得」「データ出力」2カードで、下部の横罫線（border-top）の高さを揃える。
 * 左フッターは全データ一括＋取消＋進捗文言で高くなりがちなため、両方に同じ min-height を与えて横位置を揃える。
 * 文言が極端に長くフッターがこの値を超えた場合は再調整する。
 */
export const REPORTS_CARD_FOOTER_MIN_HEIGHT_PX = 176;
