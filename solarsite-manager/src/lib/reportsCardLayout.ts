/**
 * レポート画面の「データ取得」「データ出力」2カードで、下部の横罫線（border-top）の高さを揃える。
 * 一括行＋取消ボタン＋進捗の 1 行（11px・line-height 1.5 想定）分を確保し、それ以上の余白は置かない。
 * 文言が複数行に折り返す場合はフッターがこの値を超えて伸びる。
 */
export const REPORTS_CARD_FOOTER_MIN_HEIGHT_PX = 106;
